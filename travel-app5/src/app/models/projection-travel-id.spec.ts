import { ProjectionTravelId } from './projection-travel-id';

describe('ProjectionTravelId', () => {
  it('should create an instance', () => {
    expect(new ProjectionTravelId()).toBeTruthy();
  });
});
